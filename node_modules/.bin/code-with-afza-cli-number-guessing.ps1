#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir//usr/bin/enov$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir//usr/bin/enov$exe" node "$basedir/../code-with-afza-cli-number-guessing/index.js" $args
  } else {
    & "$basedir//usr/bin/enov$exe" node "$basedir/../code-with-afza-cli-number-guessing/index.js" $args
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "/usr/bin/enov$exe" node "$basedir/../code-with-afza-cli-number-guessing/index.js" $args
  } else {
    & "/usr/bin/enov$exe" node "$basedir/../code-with-afza-cli-number-guessing/index.js" $args
  }
  $ret=$LASTEXITCODE
}
exit $ret
